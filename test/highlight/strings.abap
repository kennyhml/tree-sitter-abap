|{ attr WIDTH = 6 ALIGN = RIGHT }|
"<- string
"  ^ variable
"       ^ variable.parameter.builtin
"                  ^ variable.parameter.builtin
|{ `UPPER CASE, lower case ` CASE = (<case>) }| 
"     ^ string
"                            ^ variable.parameter.builtin


`ABAP` && `is` && 'a' && 'language'
" ^ string
"          ^ string
"                  ^ string
"                          ^ string
`string` && -1 
" ^ string
"            ^ number
