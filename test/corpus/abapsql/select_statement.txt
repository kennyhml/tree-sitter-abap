=================================================================================
[ ABAP - Database Access / ABAP SQL / SELECT ] Basic form
=================================================================================
SELECT foo FROM baz INTO @result.
---

(source
  (select_statement
    (select_list
      (identifier))
    (from_database_source_spec
      source: (identifier))
    (select_into_spec
      (select_result_struct
        work_area: (sql_host_variable
          variable: (identifier))))))

=================================================================================
[ ABAP - Database Access / ABAP SQL / SELECT ] Wildcard select
=================================================================================
SELECT * FROM baz INTO TABLE @results.
---

(source
  (select_statement
    (select_list
      (select_wildcard))
    (from_database_source_spec
      source: (identifier))
    (select_into_spec
      (select_result_table
        table: (sql_host_variable
          variable: (identifier))))))

=================================================================================
[ ABAP - Database Access / ABAP SQL / SELECT ] Select multiple fields
=================================================================================
SELECT foo, bar, baz FROM source INTO (@result_one, @result_two, @result_three).
---

(source
  (select_statement
    (select_list
      (identifier)
      (identifier)
      (identifier))
    (from_database_source_spec
      source: (identifier))
    (select_into_spec
      (select_result_targets
        (sql_host_variable
          variable: (identifier))
        (sql_host_variable
          variable: (identifier))
        (sql_host_variable
          variable: (identifier))))))

=================================================================================
[ ABAP - Database Access / ABAP SQL / SELECT ] Obsolete space-separated fields
=================================================================================
SELECT foo bar source~baz FROM source INTO (@first, @second, @third).
---

(source
  (select_statement
    (select_list
      (identifier)
      (identifier)
      (qualified_field
        source: (identifier)
        target: (identifier)))
    (from_database_source_spec
      source: (identifier))
    (select_into_spec
      (select_result_targets
        (sql_host_variable
          variable: (identifier))
        (sql_host_variable
          variable: (identifier))
        (sql_host_variable
          variable: (identifier))))))

=================================================================================
[ ABAP - Database Access / ABAP SQL / SELECT ] Explicit fields syntax
=================================================================================
SELECT FROM foo FIELDS bar, baz INTO @result.
---

(source
  (select_statement
    (from_database_source_spec
      source: (identifier))
    (select_fields_spec
      (select_list
        (identifier)
        (identifier)))
    (select_into_spec
      (select_result_struct
        work_area: (sql_host_variable
          variable: (identifier))))))

=================================================================================
[ ABAP - Database Access / ABAP SQL / SELECT ] Distinct fields
=================================================================================
SELECT DISTINCT foo, bar, baz FROM source
  INTO CORRESPONDING FIELDS OF @result.
---

(source
  (select_statement
    (select_list
      (distinct)
      (identifier)
      (identifier)
      (identifier))
    (from_database_source_spec
      source: (identifier))
    (select_into_spec
      (select_result_struct
        (corresponding_fields_of)
        work_area: (sql_host_variable
          variable: (identifier))))))

=================================================================================
[ ABAP - Database Access / ABAP SQL / SELECT ] Fully qualified fields
=================================================================================
SELECT source~bar, source~baz FROM source
  APPENDING CORRESPONDING FIELDS OF TABLE @results PACKAGE SIZE 10.
---

(source
  (select_statement
    (select_list
      (qualified_field
        source: (identifier)
        target: (identifier))
      (qualified_field
        source: (identifier)
        target: (identifier)))
    (from_database_source_spec
      source: (identifier))
    (select_appending_spec
      (select_result_table
        (corresponding_fields_of)
        table: (sql_host_variable
          variable: (identifier))
        (package_size_spec
          size: (number))))))

=================================================================================
[ ABAP - Database Access / ABAP SQL / SELECT ] Source wildcard
=================================================================================
SELECT source~* FROM source INTO TABLE @results.
---

(source
  (select_statement
    (select_list
      (qualified_field
        source: (identifier)
        target: (select_wildcard)))
    (from_database_source_spec
      source: (identifier))
    (select_into_spec
      (select_result_table
        table: (sql_host_variable
          variable: (identifier))))))

=================================================================================
[ ABAP - Database Access / ABAP SQL / SELECT ] Dynamic fields
=================================================================================
SELECT (columns) FROM source INTO @result.
---

(source
  (select_statement
    (select_list
      (dynamic_spec
        value: (identifier)))
    (from_database_source_spec
      source: (identifier))
    (select_into_spec
      (select_result_struct
        work_area: (sql_host_variable
          variable: (identifier))))))

=================================================================================
[ ABAP - Database Access / ABAP SQL / SELECT ] Single row for update
=================================================================================
SELECT SINGLE FOR UPDATE field FROM source INTO @result.
---

(source
  (select_statement
    (single)
    (for_update)
    (select_list
      (identifier))
    (from_database_source_spec
      source: (identifier))
    (select_into_spec
      (select_result_struct
        work_area: (sql_host_variable
          variable: (identifier))))))

=================================================================================
[ ABAP - Database Access / ABAP SQL / SELECT ] Select loop
=================================================================================
SELECT field FROM source INTO @result.
ENDSELECT.
---

(source
  (select_statement
    (select_list
      (identifier))
    (from_database_source_spec
      source: (identifier))
    (select_into_spec
      (select_result_struct
        work_area: (sql_host_variable
          variable: (identifier))))))
